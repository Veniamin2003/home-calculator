import React from 'react';
import {Layout} from "../Layout";
import HeatingContainer from "../Components/Heating/HeatingContainer";

const HeatingTypesPage = () => {
    return (
        <Layout>
            <HeatingContainer />
        </Layout>
    );
};

export default HeatingTypesPage;